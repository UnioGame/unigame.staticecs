# UniGame Static ECS

Runtime infrastructure for UniGame projects built on Static ECS.

This package is the replacement target for LeoEcs Proto bootstrap/tooling concepts that are still useful after review:

- typed world bootstrap conventions;
- feature composition;
- update queue orchestration;
- lifecycle helpers;
- explicit resources/services integration.

It must stay Static ECS-native and must not emulate LeoEcs Proto APIs.
